if
===
SourceFile@0..2
  IfThenElseExpr@0..2
    IfKw@0..2 "if"
    IfExpr@2..2
    ThenExpr@2..2
    ElseExpr@2..2
error at position 2 while parsing an if-then-else expression. Missing expected integer, fractional, string, char, identifier, ‘-’, ‘(’, ‘if‘, ‘match‘ or ‘|’
error at position 2 while parsing an if-then-else expression. Missing expected ‘then‘
error at position 2 while parsing an if-then-else expression. Missing expected integer, fractional, string, char, identifier, ‘-’, ‘(’, ‘if‘, ‘match‘ or ‘|’
error at position 2 while parsing an if-then-else expression. Missing expected ‘else‘
error at position 2 while parsing an if-then-else expression. Missing expected integer, fractional, string, char, identifier, ‘-’, ‘(’, ‘if‘, ‘match‘ or ‘|’
