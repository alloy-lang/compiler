typedef Type1 = Thing

trait Trait1 where
    typeof test : Int

    -- TODO: should be able to resolve abstract trait members
    let thing = test
end

behavior Trait1 for Type1 where
    typeof test : Int
    let test = 0
end
===
HirTypedModule {
    expression_types: {
        Idx::<Expression>(0): Generic(
            0,
        ),
        Idx::<Expression>(1): BuiltIn(
            Int,
        ),
    },
    pattern_types: {},
    warnings: [],
    errors: [
        TypeInferenceError {
            kind: TypeResolutionError(
                UnknownExpressionReference {
                    source_ref: Fql {
                        module_id: ModuleId(
                            Id(4),
                        ),
                        local_id: Idx::<Expression>(0),
                    },
                    module_id: ModuleId(
                        Id(4),
                    ),
                    path: NonEmpty(
                        [
                            "test",
                        ],
                    ),
                },
            ),
            range: 69..86,
        },
    ],
}
[]
