typedef Type1 = Thing

trait Trait1 where
    typeof test : self -> String -> Int

    -- TODO: should be able to resolve abstract trait members
    let thing = test
end

behavior TestTrait1 for Type1 where
    typeof test : self -> String -> Int
end
===
HirTypedModule {
    expression_types: {
        Idx::<Expression>(0): Generic(
            0,
        ),
    },
    pattern_types: {},
    warnings: [],
    errors: [
        TypeInferenceError {
            kind: TypeResolutionError(
                UnknownTraitReference {
                    source_ref: Fql {
                        module_id: ModuleId(
                            Id(4),
                        ),
                        local_id: Idx::<TypeReference>(5),
                    },
                    module_id: ModuleId(
                        Id(4),
                    ),
                    path: NonEmpty(
                        [
                            "TestTrait1",
                        ],
                    ),
                },
            ),
            range: 171..250,
        },
    ],
}
[]
