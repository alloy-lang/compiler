import test_data::test
import std::bool::Bool::{True, False}

match test when
  \ -> 0
  \ False -> 1
===
HirTypedModule {
    expression_types: {
        Idx::<Expression>(0): Missing,
        Idx::<Expression>(2): BuiltIn(
            Int,
        ),
        Idx::<Expression>(1): BuiltIn(
            Int,
        ),
        Idx::<Expression>(3): BuiltIn(
            Int,
        ),
    },
    pattern_types: {
        Idx::<Pattern>(0): Missing,
        Idx::<Pattern>(1): BuiltIn(
            Bool,
        ),
    },
    warnings: [],
    errors: [
        TypeInferenceError {
            kind: UnificationError(
                TypeMismatch(
                    Concrete(
                        Bool,
                    ),
                    Missing,
                ),
            ),
            range: 91..97,
        },
    ],
}
[
    ParseError {
        expected: [
            Integer,
            Fraction,
            String,
            Char,
            Ident,
            Minus,
            LParen,
            NilIdentifier,
        ],
        kind: Missing {
            offset: 82,
        },
        context: MatchTargetCondition,
    },
]
