import test_data

trait Trait1 where
    --empty
end

behavior Trait1 for test_data::Test where
    -- imagine all the things here
end
===
HirTypedModule {
    expression_types: {},
    pattern_types: {},
    warnings: [],
    errors: [
        TypeInferenceError {
            kind: TypeResolutionError(
                UnknownModule {
                    source_ref: TypeReference(
                        Fql {
                            module_id: ModuleId(
                                Id(4),
                            ),
                            local_id: Idx::<TypeReference>(1),
                        },
                    ),
                    module_slug: "test_data",
                },
            ),
            range: 54..134,
        },
    ],
}
[]
