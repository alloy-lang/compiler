trait TestTrait1 where
    self

    typeof distraction : String
end
===
HirTypedModule {
    module_id: ModuleId {
        path: "main",
    },
    expression_types: {},
    pattern_types: {},
    warnings: [],
    errors: [],
    poly_instantiations: {},
}
[E11001] Error: Missing expected token while parsing the `=` sign in a trait `self` constraint
   ╭─[ ./test/main.alloy:4:5 ]
   │
 4 │     typeof distraction : String
   │     │ 
   │     ╰─ expected `‘=’`
   │ 
   │ Help: While parsing the `=` sign in a trait `self` constraint, expected `‘=’`
───╯

[E11001] Error: Missing expected token while parsing the constraints for a type variable
   ╭─[ ./test/main.alloy:4:5 ]
   │
 4 │     typeof distraction : String
   │     │ 
   │     ╰─ expected `‘#’` or `identifier`
   │ 
   │ Help: While parsing the constraints for a type variable, expected `‘#’` or `identifier`
───╯


