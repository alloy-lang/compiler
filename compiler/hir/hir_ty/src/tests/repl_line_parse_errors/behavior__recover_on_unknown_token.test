typedef Type1 = Thing

trait TestTrait1 where
    -- imagine all the things here
end

behavior TestTrait1 for Type1 where
    unknown

    typevar t1
end
===
HirTypedModule {
    module_id: ModuleId {
        path: "main",
    },
    expression_types: {},
    pattern_types: {},
    warnings: [],
    errors: [],
    poly_instantiations: {},
}
[E11002] Error: Unexpected token `identifier` while parsing the start of a behavior member, are you missing a keyword?
   ╭─[ ./test/main.alloy:8:5 ]
   │
 8 │     unknown
   │     ───┬───  
   │        ╰───── unexpected `identifier`, expected `‘typeof‘`, `‘let’` or `‘typevar‘`
   │ 
   │ Help: While parsing the start of a behavior member, are you missing a keyword?, expected `‘typeof‘`, `‘let’` or `‘typevar‘` but found `identifier`
───╯


