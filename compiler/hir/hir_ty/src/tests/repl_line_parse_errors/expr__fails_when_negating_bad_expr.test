-where
===
HirTypedModule {
    module_id: ModuleId {
        path: "main",
    },
    expression_types: {
        Idx::<Expression>(0): Generic(
            0,
            "a0",
        ),
    },
    pattern_types: {},
    warnings: [],
    errors: [],
    poly_instantiations: {},
}
[E11002] Error: Unexpected token `‘where‘` while parsing an expression after a prefix operator
   ╭─[ ./test/main.alloy:1:2 ]
   │
 1 │ -where
   │  ──┬──  
   │    ╰──── unexpected `‘where‘`, expected `integer`, `fraction`, `string`, `char`, `identifier`, `operator identifier`, `‘-’`, `‘(’`, `‘if‘`, `‘match‘` or `‘|’`
   │ 
   │ Help: While parsing an expression after a prefix operator, expected `integer`, `fraction`, `string`, `char`, `identifier`, `operator identifier`, `‘-’`, `‘(’`, `‘if‘`, `‘match‘` or `‘|’` but found `‘where‘`
───╯


