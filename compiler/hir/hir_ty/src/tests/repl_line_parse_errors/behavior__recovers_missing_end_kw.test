typedef Type1 = Thing

behavior TestTrait1 for Type1 where
    -- imagine all the things here

trait TestTrait1 where
    -- imagine all the things here
end
===
HirTypedModule {
    module_id: ModuleId {
        path: "main",
    },
    expression_types: {},
    pattern_types: {},
    warnings: [],
    errors: [],
    poly_instantiations: {},
}
[E11001] Error: Missing expected token while parsing the `end` keyword after a behavior definition
   ╭─[ ./test/main.alloy:3:36 ]
   │
 3 │ behavior TestTrait1 for Type1 where
   │                                    │ 
   │                                    ╰─ expected `‘end‘`
   │ 
   │ Help: While parsing the `end` keyword after a behavior definition, expected `‘end‘`
───╯


