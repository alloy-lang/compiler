(,)
===
HirTypedModule {
    module_id: ModuleId {
        path: "main",
    },
    expression_types: {
        Idx::<Expression>(0): Generic(
            0,
            "a0",
        ),
    },
    pattern_types: {},
    warnings: [],
    errors: [],
    poly_instantiations: {},
}
[E11002] Error: Unexpected token `‘,’` while parsing an expression inside parentheses
   ╭─[ ./test/main.alloy:1:2 ]
   │
 1 │ (,)
   │  ┬  
   │  ╰── unexpected `‘,’`, expected `integer`, `fraction`, `string`, `char`, `identifier`, `operator identifier`, `‘-’`, `‘(’`, `‘if‘`, `‘match‘` or `‘|’`
   │ 
   │ Help: While parsing an expression inside parentheses, expected `integer`, `fraction`, `string`, `char`, `identifier`, `operator identifier`, `‘-’`, `‘(’`, `‘if‘`, `‘match‘` or `‘|’` but found `‘,’`
───╯


