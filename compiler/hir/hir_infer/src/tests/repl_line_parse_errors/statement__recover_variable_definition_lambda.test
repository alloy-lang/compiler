let add1 = |a, 9| -
let add2 = |a, b| -> a + b
===
DefinitionInferenceResult {
    definition_type: Unconstrained,
    expression_types: {
        Idx::<Expression>(0): Generic(
            1,
            "b0",
        ),
        Idx::<Expression>(1): Lambda {
            arg_type: Generic(
                0,
                "a0",
            ),
            return_type: Lambda {
                arg_type: BuiltIn(
                    Int,
                ),
                return_type: Generic(
                    1,
                    "b0",
                ),
            },
        },
        Idx::<Expression>(2): Generic(
            0,
            "a0",
        ),
        Idx::<Expression>(3): Generic(
            0,
            "a0",
        ),
        Idx::<Expression>(4): Generic(
            0,
            "a0",
        ),
        Idx::<Expression>(5): Lambda {
            arg_type: Generic(
                0,
                "a0",
            ),
            return_type: Lambda {
                arg_type: Generic(
                    0,
                    "a0",
                ),
                return_type: Generic(
                    0,
                    "a0",
                ),
            },
        },
    },
    pattern_types: {
        Idx::<Pattern>(0): Generic(
            0,
            "a0",
        ),
        Idx::<Pattern>(1): BuiltIn(
            Int,
        ),
        Idx::<Pattern>(2): Generic(
            0,
            "a0",
        ),
        Idx::<Pattern>(3): Generic(
            0,
            "a0",
        ),
    },
    variant_constructor_types: {},
    warnings: [],
    errors: [],
}
[E11002] Error: Unexpected token `‘-’` while parsing the `->` in a lambda expression
   ╭─[ ./test/main.alloy:1:19 ]
   │
 1 │ let add1 = |a, 9| -
   │                   ┬  
   │                   ╰── unexpected `‘-’`, expected `‘->’`
   │ 
   │ Help: While parsing the `->` in a lambda expression, expected `‘->’` but found `‘-’`
───╯

[E11001] Error: Missing expected token while parsing the body of a lambda expression
   ╭─[ ./test/main.alloy:2:1 ]
   │
 2 │ let add2 = |a, b| -> a + b
   │ │ 
   │ ╰─ expected `integer`, `fraction`, `string`, `char`, `identifier`, `operator identifier`, `‘-’`, `‘(’`, `‘if‘`, `‘match‘` or `‘|’`
   │ 
   │ Help: While parsing the body of a lambda expression, expected `integer`, `fraction`, `string`, `char`, `identifier`, `operator identifier`, `‘-’`, `‘(’`, `‘if‘`, `‘match‘` or `‘|’`
───╯


